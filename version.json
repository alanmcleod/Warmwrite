{
  "version": "2.0.2",
  "notes": [
    "Save / Share menu replaces the old Export label",
    "Share as RTF uses the device share sheet where supported",
    "Print opens the normal system print dialog",
    "Falls back to saving an RTF file when sharing is unavailable",
    "Retains the mobile keyboard dock fix"
  ]
}
