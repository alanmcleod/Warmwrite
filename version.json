{
  "version": "2.2.1",
  "notes": [
    "Fixes overlapping and clipped columns in iPhone Reading Mode",
    "Each page now uses the visible screen width with correct inner margins",
    "Writing mode and existing editor behaviour are unchanged",
    "Retains instant page changes, pen return control and reading text-size controls"
  ]
}
